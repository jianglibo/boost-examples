{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Quartz npx nodemon -L --watch content/ --ext md  --exec 'npx quartz build --serve'",
            "type": "shell",
            "command": "/bin/bash -i -c 'source ~/.bashrc && echo \"T_VAR_BASHRC: $T_VAR_BASHRC , T_VAR_PROFILE: $T_VAR_PROFILE T_VAR_BASH_PROFILE: $T_VAR_BASH_PROFILE T_VAR_DIRENV: $T_VAR_DIRENV\"'",
            // "args": [
            //     "-i", // This ensures that the shell is interactive. 
            //     "-l", // This ensures that the shell is login shell, which sources your shell configuration (e.g., .bashrc)
            //     "-c",
            //     // "source $HOME/.nvm/nvm.sh && nvm use --lts && npm run serve" // Replace '14' with the version you want
            //     "source $HOME/.bashrc && echo \"T_VAR_BASHRC: $T_VAR_BASHRC , T_VAR_PROFILE: $T_VAR_PROFILE T_VAR_BASH_PROFILE: $T_VAR_BASH_PROFILE T_VAR_DIRENV: $T_VAR_DIRENV\""
            // ],
            "presentation": {
                "panel": "new"
            },
            "problemMatcher": []
        }
    ]
}